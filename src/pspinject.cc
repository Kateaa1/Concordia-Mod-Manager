#include <filesystem>
#include <iostream>

/* This is needs to actually be written lmao
 * what this should do is copy things from the mods directory to the extracted folder like
 * mods/sysdir -> p1-extracted/PSP_GAME/SYSDIR/ 
 * for example */

void psp_inject()
{
    std::cout << "Installing mods\n";
}
